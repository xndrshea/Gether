#include "imports/stdlib.fc";


() main(slice in_msg_body) {
    int OP_TEST = 1;
    int ERR_SIMPLE = 100;

    var op = in_msg_body~load_uint(32);

    if (op == OP_TEST) {
        throw(ERR_SIMPLE);
    }
}
